/**
 * Contains the interface that users of the SDK should implement to support the caching of the results
 * of encryption operations, which can then be read to speed-up PSI calculations that rely on the same inputs and keys.
 */
package psi.cache;